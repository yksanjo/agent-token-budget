{
  "name": "agent-token-budget",
  "version": "1.0.0",
  "description": "Pre-execution cost estimator with hard limits, alerting, and automatic fallback to cheaper approaches",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "ts-node examples/main.ts"
  },
  "keywords": ["ai", "agents", "token-budget", "cost-estimation"],
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0",
    "ts-node": "^10.9.0"
  }
}
